/**
 * REST API routes for the Laminark visualization.
 *
 * Provides endpoints for graph data, timeline data, and node details.
 * All endpoints read from the better-sqlite3 database passed via Hono context.
 *
 * @module web/routes/api
 */

import { Hono } from 'hono';

export const apiRoutes = new Hono();

// Endpoints will be populated in Task 2
